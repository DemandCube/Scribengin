apply plugin: 'maven-publish'

archivesBaseName = 'scribengin.registry'

eclipse {
  project {
    name = 'scribengin.registry'
  }
}

dependencies {
  compile project(':server')
  compile group: 'com.mycila', name: 'mycila-jmx', version: '1.1.ga'
}
