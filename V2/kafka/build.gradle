apply plugin: 'maven-publish'

archivesBaseName = 'scribengin.kafka'

eclipse {
  project {
    name = 'scribengin.kafka'
  }
}

dependencies {
  compile project(':server')
  testCompile group: 'org.tap4j', name: 'tap4j', version: '4.1'
}
